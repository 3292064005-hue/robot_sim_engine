# Non-runtime compatibility stub.
#
# Repository-managed runtime baselines live in configs/profiles/*.yaml.
# The default runtime path does not consume this file; it remains only to preserve
# legacy entry points without creating a second runtime truth source.
# Prefer configs/local/solver.local.yaml or ROBOT_SIM_SOLVER_CONFIG_OVERRIDE instead.
{}
