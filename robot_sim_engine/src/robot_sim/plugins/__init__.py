"""Research-profile plugin fixtures shipped with the repository.

These modules are intentionally small and deterministic. They exist so plugin discovery is
continuously exercised by tests and profiles instead of remaining a dormant code path.
"""
